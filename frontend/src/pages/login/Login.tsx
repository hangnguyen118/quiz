import classes from './Login.module.css'

export default function Login() {
  const title = "Nonetheless, since this chapter is about JSX, it is worth diving into the syntax for outputting dynamic";
  return (
    <div className={classes.page}>
      <div className={classes.container}>
        <h1>{title}</h1>
        <form action="">
        </form>
      </div>

    </div>
  )
}
